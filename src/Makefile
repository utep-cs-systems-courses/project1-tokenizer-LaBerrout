# tokenizer Makefile #

# Objects 
OBJS = uimain.c

# First target is built by default. Usually named "all"
all: tokens

# tokenizer_out (target)
tokens: $(OBJS)
	cc -o tokens $(OBJS)

# delete files generated by compilation
clean:
	rm -f *.o tokens

run:tokens
	./tokens

